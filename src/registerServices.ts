// import CommunicationService from "/path/to/service";

import appsettings from './appsettings.json'

export async function RegisterServices() {
    // if (!appsettings.UseRedis) return;
    // try {
    //   await CommunicationService.Connect({
    //     Host: "localhost",
    //     Port: 6379,
    //   });
    // } catch (err) {
    //   console.log("ERROR!!!");
    //   console.log(err);
    //   process.exit(1);
    // }
}
